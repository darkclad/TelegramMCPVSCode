# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# Secrets and local config
config.toml

# Per-machine config (absolute install paths, deploy choices) — not portable
.mcp.json
.deploy-to-programs.json

# Local SQLite store
*.db
*.db-wal
*.db-shm

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Local dev throwaways (hardcoded paths, not portable)
/scripts/
